#include <stdio.h>
#include "static.h"

int main(int argc,char **argv)
{
	int n,i;
	struct list_node list[100],*node;

	printf("No. of data? ");
	scanf("%d",&n);

	for(i=0;i<n;i++) {
		node=list;
		printf("Enter data %d:",i+1);
		scanf("%d",

}
